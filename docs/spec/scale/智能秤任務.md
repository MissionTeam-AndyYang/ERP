# 資料來源

請根據以下兩份文件進行分析：

- docs\spec\scale\恆旺介面視意圖V6.pptx：APP 操作畫面 UI mockup
- docs\spec\scale\電子智能秤系統REST API_1.06.docx：APP 串接 REST API 規格文件

---

# 任務目標
1. 請先閱讀《電子智能秤系統 REST API_1.06.docx》文件，並依據內容更新 docs\spec\api\ 目錄下的 user.md 與 item.md，確保相對應的 URI 說明保持一致。
    以下為 MD 與 DOCX 欄位對應規則
    - 「API Group的名稱」 對應至 「階層1標題」
    - 「API Summary 的 Description欄位」 對應至 「階層2標題」
    - 「Basic Information 的 URL 欄位」 對應至 「URL」
    - 「Request Header」 對應至 「Header」
    - 「Query Parameters」 對應至 「Parameter」
    - 「Basic Information 的 Method 欄位」 對應至 「HTTP Method」
    -  Basic Information 的 Description欄位」 對應至 「Description」
    - 「Request Body」 對應至 「Send Data」, 
        -  Description 欄位內容 對應至 JSON 欄位後方的行尾註解
        -  Enum 欄位改寫 對應至 Appendix 中 {Enum Name}
    -  「Success Response Data」 對應至 「Response Data」
        -  Description 欄位內容 對應至 JSON 欄位後方的行尾註解
        -  Enum 欄位改寫 對應至 Appendix 中 {Enum Name}
    - 「Processing Flow」 對應至 「Processing Flow」
    - 「Database Tables Used」 對應至 「Database Tables Used」
 

2. 請針對《恆旺介面視意圖V6.pptx》中的**第一頁投影片畫面**進行分析，完成以下工作：

---

# API 對應分析

請指出此畫面應該呼叫的 REST API：

- API 名稱 / Endpoint
- HTTP Method
- Query Parameters
- Query Parameters 建議帶入的值（若可從畫面推斷）

---

# UI ↔ API 欄位對應

請解析畫面中的「清單列表（List / Table）」欄位，並完成對應：

- UI 欄位名稱
- 對應 REST API Response 欄位名稱
- 若無對應欄位，請標註：
  - `無對應` 或 `需確認`

---

# 輸出格式要求

請統一使用以下格式輸出：

## A. API 呼叫資訊

- Endpoint:
- Method:
- Query Parameters:
  - key: value（說明）

---

## B. UI 欄位對應表

| UI 欄位 | API 回傳欄位 | 備註 |
|--------|--------------|------|

---

# 額外規則

- 若 API 文件中存在多個可能 API：
  - 請列出「候選 API」
  - 並說明選擇原因
- 若 Query Parameter 無法從畫面推斷：
  - 請標註「需前端補值」
